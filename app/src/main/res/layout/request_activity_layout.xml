<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:background="@color/colorPrimary"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                tools:context=".ui.extra.RequestActivity">

                <!-- recording_button -->
<!--
                <Button android:id="@+id/recording_button"
                        android:layout_width="145dp"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/d50dp"
                        android:layout_centerHorizontal="true"
                        android:text="Start Recording" />
-->

<!--
                <Button android:id="@+id/stop_recording"
                        android:layout_below="@id/recording_button"
                        android:layout_width="145dp"
                        android:layout_marginTop="@dimen/d10dp"
                        android:layout_height="wrap_content"
                        android:layout_centerHorizontal="true"
                        android:text="Stop Recording" />
-->

    <TextView android:id="@+id/textview_to_show_reply"
              android:layout_height="700dp"
              android:layout_width="400dp"
              android:background="@color/colorPrimaryDark"
              android:textColor="@color/unnamed"
              android:layout_centerInParent="true"/>


</RelativeLayout>